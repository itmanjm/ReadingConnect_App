rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o/{allPaths=**} {
    // Allow read access to everyone (public audio files)
    allow read: if true;
    
    // Allow write access to authenticated users (teachers uploading audio/PDFs)
    allow write: if request.auth != null;
  }
}
