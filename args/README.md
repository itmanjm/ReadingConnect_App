# Args Layer

> Behavior settings that shape how the system acts.
> Changing args changes behavior without editing goals or tools.

Example args files:
- `daily_theme.yml` - Content themes for the day
- `output_format.yml` - Format preferences
- `model_choice.yml` - Which AI model to use
